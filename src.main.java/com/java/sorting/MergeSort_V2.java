package com.java.sorting;

public class MergeSort_V2 {
	/**
	 * Using Global Auxiliary Array : If you have noticed in previous case we are creating the array's for each left and right and then merging it.
	 * Now if GC not collect them immediately then at max the can be in memory and total size consume for them is nlogn
	 * 
	 * Hence to avoid this we can use one auxiliary array with size n -- ans use for all purpose instead of creating each time.
	 */
	
	
	

}
